#!/bin/bash -e
DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && cd .. && pwd )
#cd ${DIR}/youtube/opt/frigate
#export CONFIG_FILE=/var/snap/youtube/current/config/frigate/config.yaml
#export PATH=$PATH:${DIR}/youtube/bin
#rm -rf /var/snap/youtube/current/api.socket
#exec ${DIR}/youtube/bin/python.sh -u -m frigate
